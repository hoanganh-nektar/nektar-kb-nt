# nektar-kb-nt

## Git rules
- The only allowed remote is: https://github.com/hoanganh-nektar/nektar-kb-nt.git
- Never push without explicit confirmation from the user.
- Always run `git remote -v` before pushing to verify the remote URL.
